# <Secure Notes App> — Module 3
**Goal:** Build a login‑protected Notes API where users authenticate with Amazon Cognito and perform CRUD on notes via API Gateway + Lambda + DynamoDB.
**Architecture:** CLI client, cognito user pool, api gateway with a cognito authorizer, notes handler in lambda, dynamodb table to store notes.
**How to run:**
**Acceptance checklist:**
**Portfolio deliverables:**
- Screenshot: Cognito pool & app client.
- Screenshot: Authorizer and a method using it.
- Screenshot: DDB items (note rows).
- curl/Postman responses for 201/200/404.
**Cleanup:**

